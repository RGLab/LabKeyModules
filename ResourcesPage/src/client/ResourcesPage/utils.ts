
// Event key for bootstrap TabContainer
export const generateChildId = (eventKey: any, type: any) => {
    return eventKey;
}

// Resolves warnings in bootstrap TabContainer
export const noop = () => {}